use crate::formation::*;
use bevy::{pbr::NotShadowCaster, prelude::*};
use bevy_mod_picking::prelude::*;

pub struct FormationControlsKeyboardPlugin;

impl Plugin for FormationControlsKeyboardPlugin {
    fn build(&self, _app: &mut App) {}
}
